# 修改为 .env.local 来使用捏

# AI 提供商配置（JSON 格式）
# 按顺序配置多个提供商，每个配置一个模型
AI_PROVIDERS_CONFIG='[
  {
    "name": "gemini_primary",
    "apiKey": "your_gemini_api_key_here",
    "baseUrl": "https://generativelanguage.googleapis.com/v1beta",
    "model": ["gemini-2.0-flash", "gemini-1.5-pro", "gemini-1.5-flash"],
    "type": "google",
    "weight": 3,
    "retryCount": 2,
    "skipProbability": 0
  },
  {
    "name": "openai_provider",
    "apiKey": "your_openai_api_key_here",
    "baseUrl": "https://api.openai.com/v1",
    "model": ["gpt-4o", "gpt-4o-mini", "gpt-4-turbo"],
    "type": "openai",
    "weight": 2,
    "retryCount": 1,
    "skipProbability": 0.1,
    "mode": "json"
  },
  {
    "name": "backup_gemini",
    "apiKey": "your_backup_gemini_key",
    "baseUrl": "https://backup-api.com/v1",
    "model": "gemini-1.5-flash",
    "type": "google",
    "weight": 1,
    "retryCount": 1,
    "skipProbability": 0.3
  }
]'

# 原生数据验证功能：HMAC签名的密钥配置
SIGNATURE_SECRET_KEY="在这里填写一个长且随机的安全密钥"

# 队列系统配置
# 每分钟最大API请求数（控制向AI服务器的请求频率）
QUEUE_MAX_REQUESTS_PER_MINUTE=10
# 用户等待时间（秒，同一用户两次请求之间的最小间隔时间）
QUEUE_USER_WAIT_TIME_SECONDS=30

# AI负载均衡配置
# 负载均衡策略: sequential(顺序) | random(随机) | round_robin(轮询)
AI_LOAD_BALANCE_STRATEGY=random

# GM回合两段式生成配置
# same-session为默认值，表示在同一模型会话内完成两段式；
# separate-model表示第二阶段优先使用轻量模型进行结构化；disabled可回退至单次生成。
GM_TURN_TWO_STAGE_MODE=same-session
# 指定第二阶段格式化任务的模型优先顺序（JSON数组或逗号分隔字符串），可选。
GM_TURN_FORMATTING_MODEL_PRIORITY='["gemini-2.5-flash-lite", "gemini-2.0-flash", "gemini-2.5-flash"]'

# Cloudflare D1 数据库配置
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id_here
D1_DATABASE_ID=your_d1_database_id_here

NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# 用户系统的验证码服务
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your_site_key_here
TURNSTILE_SECRET_KEY=your_secret_key_here
